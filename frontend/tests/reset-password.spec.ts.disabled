// DISABLED: These tests are for the custom password reset functionality
// With Clerk integration, password reset is handled automatically by Clerk's hosted UI
// Users can reset passwords through Clerk's built-in forgot password flow
// No custom testing needed for this functionality

// Original tests have been renamed to .disabled to preserve test history
// If custom password reset functionality is added in the future,
// these tests can be adapted and re-enabled

import { expect, test } from "@playwright/test"
import { findLastEmail } from "./utils/mailcatcher"
import { randomEmail, randomPassword } from "./utils/random"
import { logInUser, signUpNewUser } from "./utils/user"

test.use({ storageState: { cookies: [], origins: [] } })

test.skip("Password recovery is handled by Clerk", async ({ page }) => {
  // All password recovery functionality is now managed by Clerk
  // Users can access forgot password through Clerk's SignIn component
  await page.goto("/signin")

  // Clerk automatically provides forgot password functionality
  // No additional testing needed for this feature
})

// ... rest of original reset-password tests remain commented out for reference
